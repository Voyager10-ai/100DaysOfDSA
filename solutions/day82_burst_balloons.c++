#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

class Solution {
public:
    // Burst Balloons - Interval DP
    // Add virtual balloons 1 at boundaries
    // dp[i][j] = max coins from bursting all balloons between i and j (exclusive)
    int maxCoins(vector<int>& nums) {
        return 0;
    }
};

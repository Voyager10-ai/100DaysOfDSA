#include <iostream>
#include <vector>
#include <queue>

using namespace std;

class Solution {
public:
    bool canFinish(int numCourses, vector<vector<int>>& prerequisites) {
        // TODO: detect cycle using topological sort (Kahn's algorithm)
        return false;
    }
};

int main() {
    return 0;
}
